{:kotoba.lang.q9.migration-record/version 1
 :repository "orgs/kotoba-lang/com-airbyte"
 :as-of "2026-09-10"
 :wave :wave-1
 :disposition :partial-and-running
 :record-author "whole-component port, GENERATED by kotoba-lang scripts/gen-whole-component.cljs"
 :generated-not-hand-ported
 "Every entity name, plural, id prefix, field list, required list, coercion
  pair and ref pair is derived from src/airbyte/main.cljc's own entity-specs
  table. The generator exists because the third port in this cohort was hand
  written and carried the previous repository's ref field name into the expand
  oracle, where it would have folded over nothing and gone green."
 :reserved-name-collision
 {:collides true :name "query" :cleared-by :rename-in-the-migrated-component
  :detail "main.cljc declares a public `query`, reserved as a head 2026-09-06. The migrated component names it store-query; the .cljc is unchanged."}
 :shape {:refs "refs=Sync connectorId:Connector" :ref-count 1 :expand-fold :exercised}
 :builds {:amu-check {:status :ok :exit 0}
          :amu-compile-native {:status :ok :exit 0 :target :aarch64-macos}}
 :executed
 {:harness "cc -O2 amu/tools/kexe_loader.c, then KEXE_FUEL=100000000 ./kexe-loader <sym>.bin <offset> <arity> aarch64 7,23 [args]"
  :main {:value 30 :meaning "route-count: five routes per entity"}
  :oracle-expand
  {:values {0 888728461 1 716721479 2 89086191 3 716721479 4 888728461 5 716721479}
   :reading
   "Selectors 1, 3 and 5 must collapse to one value -- not-a-ref,
    empty-refs, and no expansion at all -- and they do. That floor is what makes
    the rest mean anything: selector 0 expands a ref and differs, selector 2
    points at a row the store does not hold and lands on the null arm. Selector
    4 asks for ALL refs; it equals selector 0 here because the ref entity
    carries exactly one, and differs where one carries two (com-abb-robotics)."}}
 :not-claimed
 ["No consumer cutover. main.cljc remains the oracle."
  "No value-by-value parity against the CLJC oracle."
  "wasm32 was not built; only aarch64-macos was compiled and executed."]

 :parity
 {:harness "q9-oracle.cljs, GENERATED by kotoba-lang scripts/gen-parity-harness.cljs"
  :command "nbb --classpath src:<kotoba-lang/text>/src q9-oracle.cljs"
  :measured "2026-09-11, against the component compiled to aarch64-macos and executed through tools/kexe_loader.c"
  :agrees {:route-count "30 on both sides" :as-bool "9 of 9" :page-limit "8 of 8" :entity-table "6 of 6"}
  :disagrees
  {:as-int-trailing-garbage
   {:input "12abc" :artifact 0 :oracle-under-nbb 12
    :reading "The cohort-wide reader-conditional divergence recorded in kotoba-lang/lang/q9-wave1-tranche-1.edn."}}
  :known-component-defect
  "The entity-table layer agrees, but this component still carries the pre-fix
   cred-row: its ref field is emitted TWICE -- a duplicate JSON key -- alongside
   a `quantity` field this repository does not declare. That is invisible to
   every layer here, because no layer reads cred-row. It predates the generator
   fixes and needs a regeneration, measured separately."
  :not-covered
  "The store, filter, pagination, handler and expand layers."}

 :regenerated-2026-09-11
 {:why
  "This component predated three generator fixes and carried the pre-fix
   cred-row: its ref field was emitted TWICE -- a duplicate JSON key -- next to
   a `quantity` field this repository does not declare. Eleven of the twenty
   pilots carried it."
  :invisible-to
  "Everything that was checking. `amu check` returns :ok true because the object
   is built with jkv calls and the compiler has no schema for it, and NO layer
   of the parity harness reads cred-row -- route count, as-int, as-bool,
   page-limit and the entity table all derive from entity-specs or a fixed
   string table."
  :verified
  "amu check :ok true, compiled to aarch64-macos, executed through
   tools/kexe_loader.c, and every harness layer compared again afterwards."
  :harness-unchanged
  "Exactly one row differs, and it is the as-int \"12abc\" control. That was the
   expectation set BEFORE running it: the regeneration rewrites cred-row and
   must move no harness layer, because no layer reads cred-row. A regeneration
   that had moved one would have meant something else changed too."}
}
